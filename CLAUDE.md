# Assistant — телеграм-бот для сбора задач

Личный бот-трекер задач в Telegram. Пишете боту текст — он сохраняется в базе данных.
Реализован **без собственного кода** — как сценарий (workflow) в n8n, который уже
развёрнут в проекте [[n8n-hub]] (см. `../n8n-hub/CLAUDE.md`).

Общие правила вайбкодинга — в `../CLAUDE.md` (корень VS_hub). Этот файл — только
специфика проекта Assistant.

## Статус

MVP + привязка задач к совещаниям. Список задач (просмотр в боте), отметка
выполнения, напоминания по срокам — следующие шаги, каждый отдельным заходом.

## Как это работает

1. Пользователь (только владелец, Telegram ID `325113969`) пишет сообщение боту
   `@AI_kuzkin_bot`.
2. Сценарий n8n на сервере Timeweb (5.129.228.135) ловит сообщение через
   Telegram-триггер, проверяет, что писал именно владелец (иначе — тишина).
3. Дальше сообщение разбирается на тип:
   - **`/meeting_start [название]`** — начинает совещание. Без названия — имя
     ставится автоматически по дате/времени («Совещание 22.07.2026 14:30»).
   - **`/ea`** — быстрый старт типового совещания «ЕА нед» (без набора текста).
   - **`/ai`** — быстрый старт типового совещания «АИ нед».
   - **`/work`** — быстрый старт типового совещания «Work».
   - **`/meeting_end`** — завершает текущее открытое совещание (проставляет
     `ended_at`). Если открытых совещаний нет — бот отвечает «Активных совещаний нет».
   - **любой другой текст** — обычная задача: сохраняется в таблицу `tasks`; если
     на момент отправки есть незавершённое совещание — задача автоматически
     привязывается к нему (`meeting_id`), и бот об этом сообщает в ответе.
   - **любая другая команда** (начинается на `/`, но не распознана) — игнорируется
     молча, чтобы не засорять список задач служебными командами.
4. Бот отвечает подтверждением («Задача добавлена ✅», «Совещание «…» начато/завершено»)
   и показывает под полем ввода кнопки-ярлыки: «/ea», «/ai», «/work», «/meeting_end» — чтобы
   не набирать команды вручную. Нажатие на кнопку отправляет её текст как обычное
   сообщение (это стандартное поведение Telegram reply-клавиатуры).

Никакого собственного сервера/кода у бота нет — вся логика в визуальном
конструкторе n8n (веб-интерфейс: http://5.129.228.135:5678, редактировать сценарий
можно там же). Правки в схему вносятся через n8n API или через веб-интерфейс n8n
(не через файлы этого репозитория).

### Схема совещаний в базе

- Таблица `meetings` (id, name, started_at, ended_at) — одна строка на совещание;
  `ended_at IS NULL` означает, что совещание ещё открыто (максимум одно открытое
  совещание одновременно — «Найти активное» берёт самое свежее с `ended_at IS NULL`).
- В `tasks` добавлена колонка `meeting_id` (nullable, внешний ключ на `meetings.id`) —
  задачи без активного совещания сохраняются с `meeting_id = NULL`.
- В `tasks` есть колонка `tags` (text, nullable) — теги задачи строкой через запятую
  (например «СБ3, КЛАД»); заполняется только из админки (2026-08-20).

## Подключения

| Что | Значение |
|---|---|
| Бот | `@AI_kuzkin_bot`, токен — см. `../ДОСТУПЫ.md`, раздел «Телеграм-бот задач (Assistant)» |
| Сервер | Timeweb 5.129.228.135 (тот же, где n8n и VPN — см. `../n8n-hub/CLAUDE.md`) |
| База данных | Docker-контейнер `tasks-db` (postgres:16-alpine, том `tasks_pg_data`), в сети Docker `app-net` вместе с контейнером `n8n` |
| Таблицы | `tasks` (id, chat_id, user_name, text, status, created_at, done_at, meeting_id, tags, notes) и `meetings` (id, name, started_at, ended_at) в базе `tasksdb` |
| n8n workflow | id `cChSoVNfUB34jrFT`, «Task Bot — добавление задачи» |
| n8n credentials | «Task Bot Telegram» (id `iGCobtYiUUFdbEK9`), «Task Bot Postgres» (id `O7C4uvd1yrZ9uu3n`) |

Все пароли и токены — в `../ДОСТУПЫ.md`, здесь не дублируются.

## Схема сценария (n8n)

```
Telegram Trigger → Owner only (IF: from.id = 325113969) → Classify (Code: определяет тип сообщения)
  → Is meeting_start? → Insert meeting (Postgres) → Reply start
  → Is meeting_end?   → End meeting (Postgres)     → Reply end
  → Is task?          → Find active meeting (Postgres) → Save task (Postgres) → Reply task
```

- «Owner only» отсекает чужие сообщения — бот публичный (findable по @username),
  но реагирует только на владельца.
- «Classify» (JS-код) разбирает текст: `/ea`, `/ai`, `/meeting_start [имя]`,
  `/meeting_end` или обычная задача — распознаются по точным фразам, не по смыслу.
- Все запросы к базе — через параметризованные SQL-запросы Postgres-узла
  (`options.queryReplacement`, значения передаются массивом, а не склейкой строк) —
  исключает SQL-инъекции через текст задачи или название совещания.
- Узлы «End meeting» и «Find active meeting» помечены `alwaysOutputData: true` —
  если открытого совещания нет, они всё равно отдают пустой результат дальше по
  цепочке, а не обрывают выполнение.

## Известные нюансы

- Сервер общий с [[n8n-hub]] — там же живёт VPN-контейнер `amnezia-awg`. Любые
  Docker-операции (пересоздание контейнеров, новые сети) могут на секунды сбить его
  iptables. После любых изменений на этом сервере — `docker restart amnezia-awg` и
  проверка NAT-правила (см. `../n8n-hub/CLAUDE.md`).
- Вебхук Telegram работает через `https://hub.каскад.team` (Cloudflare перед сервером
  терминирует HTTPS) — то же, что использует бот Acifrui.
- Смотреть данные в базе можно так:
  ```
  ssh root@5.129.228.135 "docker exec tasks-db psql -U tasksbot -d tasksdb -c 'SELECT * FROM tasks ORDER BY id DESC LIMIT 20;'"
  ```

## Админка (просмотр, статусы, канбан)

Отдельный публичный сайт на GitHub Pages. С 2026-09-22 страница переведена на общий
дизайн-код админок Аконс (`../Дизайн_стандарт/DESIGN.md`, каркас из `starter.html`):
тёмная шапка «Аконс / Задачи» со ссылкой «‹ Портал» (https://acons.space), без блока
пользователя (вход по токену), белая прилипающая панель действий, контент до 1400 px.
Только компьютер и планшет от 1024 px, под телефон не адаптируется; тёмной темы нет.

**Панель действий** (слева направо): переключатель «Список | Канбан», фильтры
«Все статусы» (Открыта / В работе / Готово / Архив), «Все теги» (из существующих тегов),
«Все совещания» (из данных, плюс «Без совещания»), поиск по тексту задачи; справа —
«Обновлено чч:мм», круглая кнопка обновления, синяя «Добавить задачу». Фильтры и поиск
работают на клиенте и применяются к обоим видам сразу; под заголовком «Задачи» —
итог «Показано X из N» (N — без архивных; в режиме «Архив» — только архивные). Выбранный
вид, фильтры (включая «Архив»), строка поиска и сортировка запоминаются в браузере
(`localStorage`, ключ `assistant-tasks:ui`, в try/catch). Поиск ищет по тексту задачи
и по заметкам.

**Быстрый ввод** (2026-09-22): под заголовком «Задачи» в обоих видах — поле «Новая
задача: введите текст и нажмите Enter» и кнопка «Добавить» (обычная, не синяя). Enter
или кнопка создают задачу через `assistant-tasks-create` без повторов; на время запроса
поле блокируется, при успехе очищается, задача появляется сверху списка / в колонке
«Открыта», тост «Задача добавлена»; при ошибке текст остаётся в поле и показывается
красный тост. Пустой текст не отправляется, фокус после добавления остаётся в поле.
Поле скрыто, пока список не загружен (нет токена, ошибка загрузки).

**Архив** (2026-09-22): вместо удаления задача уходит в архив — кнопка «В архив» в строке
таблицы, на карточке канбана и в подвале окна задачи (смена статуса на `archived` через
`assistant-tasks-status`, без подтверждения, оптимистично с откатом, тост «Задача в
архиве»). Архивные задачи не показываются ни в списке, ни в канбане, пока в фильтре
статусов не выбран «Архив»: тогда список показывает одну таблицу только с архивными
(без группы «Готовые»), а канбан — сообщение «Архив смотрите в списке» с кнопкой
«Открыть список». У архивной задачи вместо «В архив» две кнопки: «Восстановить»
(статус `open`, тост «Задача восстановлена») и «Удалить навсегда» (`assistant-tasks-delete`
после `confirm("Удалить задачу навсегда? Восстановить будет нельзя.")`, тост «Задача
удалена»); в окне архивной задачи они же в подвале слева. «Архив» есть и в меню пилюли
статуса, и в списке статусов окна (серая пилюля с текстом «Архив»); колонки канбана
остаются три — `archived` в колонку не попадает (`BOARD_COLUMNS`, отдельно от
`STATUS_ORDER`).

**Заметки** (2026-09-22): в окне задачи под текстом поле «Заметки» (4 строки, до 2000
знаков — заметки уходят GET-параметром, длинный адрес запроса может не пройти).
Сохраняются через `assistant-tasks-notes` только если изменились, с откатом при ошибке
как у тегов; у новой задачи — отдельным запросом после создания (как статус и теги).
В таблице и на карточке под текстом задачи серым показывается первая строка заметки,
обрезанная до одной строки, без раскрытия.

- **Список** — стандартная таблица (шапка прилипает под панелью действий, строки 44 px):
  столбцы Задача (клик открывает окно), Статус (пилюля: Открыта серая, В работе жёлтая,
  Готово зелёная; клик по пилюле — маленькое меню смены статуса), Теги (капсулы; клик —
  меню с галочками по всем тегам и строкой нового тега, сохраняется сразу), Совещание,
  Создана, кнопка «В архив» (для архивных — «Восстановить» и «Удалить навсегда»).
  Сортировка щелчком по шапке столбцов Задача / Статус /
  Создана, по умолчанию новые сверху. Выполненные (вычеркнутые) задачи — внизу в группе
  «Готовые»: 10 последних, остальные за «Показать ещё N» (2026-08-21). Пустые состояния:
  «Задач нет» и «По фильтру ничего не найдено» с кнопкой «Сбросить фильтры».
- **Канбан** — три колонки Открыта / В работе / Готово на серой подложке, в заголовке
  счётчик карточек. Карточки плоские: текст (клик открывает окно), теги, дата и совещание
  серым, кнопки перехода статуса текстом («В работу»; «Готово» и «Вернуть»; «Вернуть
  в работу») — работают пальцем на планшете, и кнопка «В архив». Перетаскивание мышью
  между колонками сохранено. В колонке «Готово» — 10 последних карточек, остальные
  за «Показать ещё N» (2026-08-21).

| Параметр | Значение |
|---|---|
| Сайт | https://nick3000ept.github.io/assistant-tasks/ (нужен `?t=токен` в ссылке) |
| Репозиторий | https://github.com/Nick3000ept/assistant-tasks (публичный код, но данные закрыты токеном) |
| Backend (API) | n8n workflow id `bGf3PucODMr3T9Xi` («Assistant — API для админки задач») |
| Токен доступа | см. `../ДОСТУПЫ.md`, раздел «Телеграм-бот задач (Assistant)» — без него любой запрос отвечает 403 |
| Ночной облачный агент | см. раздел ниже «Ночной облачный агент» |

### API-эндпоинты (все — GET с `?t=токен`, без preflight/CORS-сложностей)

| Путь | Параметры | Действие |
|---|---|---|
| `/webhook/assistant-tasks` | — | Список задач (JSON), `LEFT JOIN meetings`; у каждой задачи есть поле `notes` (строка или null, 2026-09-22) |
| `/webhook/assistant-tasks-status` | `id`, `status` (`open`/`in_progress`/`done`/`archived` — архив, 2026-09-22) | Меняет статус; `done` дополнительно проставляет `done_at = now()`, любой другой статус (в том числе `archived`) сбрасывает `done_at` в NULL |
| `/webhook/assistant-tasks-delete` | `id` | Удаляет задачу безвозвратно (без корзины). С 2026-09-22 админка вызывает его только из архива («Удалить навсегда») |
| `/webhook/assistant-tasks-edit` | `id`, `text` | Меняет название (текст) задачи |
| `/webhook/assistant-tasks-tags` | `id`, `tags` | Сохраняет теги задачи (строка через запятую; пустая — очищает) |
| `/webhook/assistant-tasks-notes` | `id`, `notes` | Сохраняет заметки задачи (пустая строка очищает); сделан по образцу `-tags` (2026-09-22) |
| `/webhook/assistant-tasks-mark` | `id`, `context`, `auto_run` (`true`/`false`) | Ставит/снимает пометку для ночного агента, сохраняет контекст, сбрасывает `run_status`/`run_result` |
| `/webhook/assistant-tasks-queue` | — | Для облачного агента: задачи с `auto_run=true` и ещё не обработанные |
| `/webhook/assistant-tasks-runresult` | `id`, `run_status`, `run_result` | Для облачного агента: записать результат прогона задачи |

Базовый адрес: `https://hub.каскад.team`. Проверка токена — в каждом Webhook-узле
отдельным IF-узлом (сравнение строк), не в коде сайта — подделать запрос без
токена нельзя, даже зная структуру API.

### Как это работает
- Страница `index.html` берёт токен из своей же ссылки (`?t=...`) один раз и
  использует его для всех запросов к API.
- Список задач грузится одним запросом (`assistant-tasks`); вкладки, фильтры, поиск
  и сортировка работают на клиенте (без перезагрузки) — один и тот же список
  рисуется по-разному для «Список» и «Канбан». Кнопка обновления в панели действий
  перезапрашивает список, подпись «Обновлено чч:мм» / «Обновляю…» рядом.
- **Клик по тексту задачи** (в списке и в канбане) открывает окно «Задача» / «Новая
  задача»: текст (можно поправить), заметки, статус (выпадающий список: Открыта /
  В работе / Готово / Архив), теги (через запятую, с подсказками из существующих),
  служебное (создана, совещание, выполнена). «Сохранить» шлёт только реально
  изменившиеся поля через соответствующие эндпоинты; новая задача создаётся через
  `assistant-tasks-create`, затем при необходимости отдельно статус, теги и заметки.
  В подвале окна слева — «В архив» (для архивной задачи — «Удалить навсегда» и
  «Восстановить»), те же действия, что и кнопки в строке/карточке; несохранённые
  правки текста при этом не отправляются. Окно закрывается по Esc, крестику и клику
  по затемнению. Блок «выполнить автоматически» (контекст, тип, галочка) убран из окна
  2026-08-20 по решению пользователя — пометка для ночного агента из админки
  больше недоступна (API-эндпоинт `assistant-tasks-mark` остался).
- Все запросы идут через один помощник `api(path, params)` (2026-09-22): те же адреса
  и параметры, что раньше, токен добавляется сам; при ошибке сети или ответе 5xx —
  до двух повторов с паузой; ответы 4xx (403 — неверный токен) не повторяются.
  Создание задачи повторов не имеет (повтор мог бы завести задачу дважды) — при сбое
  окно остаётся открытым с текстом, можно нажать «Сохранить» ещё раз.
- Архивирование, восстановление, удаление, смена статуса (пилюля, кнопки на карточке,
  перетаскивание), теги и заметки — сразу оптимистично меняют вид на странице, запрос
  уходит фоном. Успех подтверждается тостом в правом нижнем углу («Сохранено»,
  «Задача добавлена», «Задача в архиве», «Задача восстановлена», «Задача удалена»,
  «Статус изменён»); если запрос не прошёл — изменение откатывается в интерфейсе
  и показывается красный тост «Нет связи, изменение не сохранено» (2026-09-22;
  раньше ошибки терялись молча).
- Правки в API — через n8n (веб-интерфейс или API), правки в вид страницы —
  через `index.html` в этом репозитории (`git push` в main публикует сам, GitHub
  Pages, ветка main, корень).

### Известные ограничения
- Безвозвратное удаление (`assistant-tasks-delete`) с 2026-09-22 доступно только из
  архива («Удалить навсегда») и только после браузерного `confirm()`; на сервере
  подтверждения и корзины нет — удалённую задачу не восстановить. Обычная кнопка
  «В архив» ничего не удаляет, задача возвращается через «Восстановить».

## Ночной облачный агент (авто-выполнение помеченных задач)

Задачу можно пометить в окне задачи (клик по тексту → контекст + «Выполнить
автоматически»). Раз в сутки, в 01:00 по Москве, отдельный облачный агент
(routine на claude.ai, id `trig_01VYsU3DimEKWD8ZUPFNiMTp`, cron `0 22 * * *`
UTC) забирает такие задачи и обрабатывает каждую по её типу (`task_type`):

- **`code`** — правка кода: читает CLAUDE.md проекта, вносит правку в отдельной
  git-ветке, открывает Pull Request. Не деплоит, не сливает.
- **`report`** — отчёт/анализ: читает GAS Web App URL проекта из его CLAUDE.md,
  запрашивает данные (`action=load`/`getData`/т.п.), считает то, что описано в
  `context`, форматирует текстом и **сразу отправляет в Telegram** (боту
  Assistant, напрямую через Bot API `sendMessage`, без похода через n8n) —
  никакого кода/PR для этого типа не создаётся.

**Важно: это ДРУГОЙ агент**, не связанный с обычными сессиями Claude Code — у
него нет доступа к этому компьютеру, серверу Timeweb (SSH/база данных напрямую)
и приватным репозиториям. Только к перечисленным ниже публичным репозиториям и к
API этого проекта через интернет.

### Что он делает
1. GET `.../webhook/assistant-tasks-queue?t=токен` — получает задачи с `auto_run=true`
   и `run_status` в (`NULL`, `queued`).
2. Для каждой — определяет проект по тексту/контексту, читает CLAUDE.md проекта,
   вносит правку **в отдельной git-ветке** (`auto-task-<id>-...`), пушит ветку,
   открывает **Pull Request** — и всё, дальше стоп: **никогда сам не сливает PR,
   не деплоит и не публикует** (ни `clasp deploy`, ни `netlify deploy`, ни merge
   в main). Слияние и деплой — вручную, пользователем, после ревью (можно попросить
   обычную сессию Claude Code «глянь PR по задаче N» — она прочитает диф и даст
   короткую сводку понятным языком, решать пользователю).
3. GET `.../webhook/assistant-tasks-runresult?t=токен&id=X&run_status=...&run_result=...`
   — пишет результат обратно (обязательно для каждой обработанной задачи):
   `pr_opened` (code, успех, + ссылка на PR), `sent` (report, успех, + короткое
   подтверждение), `needs_clarification` (+ что неясно) или `failed` (+ причина).
   Статус и ссылка видны в окне задачи в админке.

### Ограничение: приватные репозитории недоступны
Агент подключён только к публичным репозиториям: `shahmatka`, `shahmatka-sb5`,
`parking-admin`, `cc-shahmatka`, `supply-form`, `assistant-tasks`. Приватные —
`contracts-control` (Контроль договоров), `daily-digest`, `acifrui` — облачная
среда claude.ai не пускает (пробовал подключить, отказ прав доступа). Задачи по
этим проектам агент помечает `needs_clarification` с причиной «нет доступа к
репозиторию» — их можно взять только в обычной сессии Claude Code.

### Как пометить задачу
Из админки пометка убрана 2026-08-20 (блок «выполнить автоматически» удалён из
окна задачи по решению пользователя). Пометить задачу можно только напрямую
через API (`assistant-tasks-mark?id=…&auto_run=true&task_type=…&context=…`)
или правкой в базе; в контексте обязательно указывать проект — иначе агент
отметит needs_clarification.

## Планы (не сделано)

- `/list` — показать открытые задачи (в том числе сгруппированные по совещанию/дате).
- Отметка задачи выполненной (кнопка или команда).
- Напоминания по дедлайну.
- Сообщение при `/meeting_start`, если уже есть открытое совещание (сейчас можно
  открыть второе поверх первого — новые задачи уйдут во второе, первое останется
  «висеть» открытым до отдельного `/meeting_end`).
